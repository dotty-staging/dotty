dotty
=====

The experimental compiler for a Scala dialect based on DOT

```
scala compile -S 3.7.2-RC1-bin-SNAPSHOT --server=false -O="--color:never" -O="--nowarn" . &> out.txt
```

```
pipx run fix.py
```